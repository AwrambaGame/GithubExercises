using System; 
using Fraction;

namespace Program; 

    class PrintString{
        {
            string gameProject = "This is a game project";
            Console. WriteLine(gameProject);
            Fraction.Fraction fraction = new Fraction.Fraction(1,2);
            Console.WriteLine(fraction);
        }
    }
